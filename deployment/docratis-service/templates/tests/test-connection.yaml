apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docratis-service.fullname" . }}-test-connection"
  labels:
    {{- include "docratis-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "docratis-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
