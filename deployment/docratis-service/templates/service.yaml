apiVersion: v1
kind: Service
metadata:
  name: {{ include "docratis-service.fullname" . }}
  labels:
    {{- include "docratis-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docratis-service.selectorLabels" . | nindent 4 }}
